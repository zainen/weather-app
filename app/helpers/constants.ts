export enum TemperatureUnitsEnum {
  C = "°C",
  // the not even low key bad choice for tempurature TemperatureUnitsEnum...
  F = "°F"
};

export const DAY_UNIX = 60 * 60 * 24;
export const HOUR_UNIX = 60 * 60;
